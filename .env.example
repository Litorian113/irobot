# The browser never sees this key. Locally the Vite dev server reads it from
# .env and serves /api/token; on Vercel set the same variable in the project
# settings and api/token.js serves it as a serverless function. Either way the
# client only ever receives short-lived, single-use Voice Agent tokens.
ASSEMBLYAI_API_KEY=...
